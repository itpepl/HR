mirror_to_github:
  stage: mirror
  script:
    - git config --global user.name "GitLab CI"
    - git config --global user.email "ci@gitlab.com"
    - git remote set-url github "$GITHUB_REPO" || git remote add github "$GITHUB_REPO"
    - git push github HEAD:main --force